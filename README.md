# Initialize

Load the ZSH and TMUX plugins:

```./init-dotfiles.sh```

Source the ZSH config:

```source .zshrc```

When a new file is added, create a symlink in the home dir:

```stow .```

To make screenshot scrip work:

`sudo apt install jq wf-recorder grim slurp fuzzel`
